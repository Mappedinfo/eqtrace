schema_version = 1
title = "ignored-epsilon"
paper_sources = ["paper.tex"]
[[contracts]]
implementation = "implementation.py"
id = "ignored-epsilon"
label = "eq:ignored-epsilon"
function = "f"
inputs = ["x", "mu", "sigma", "epsilon"]
domains = { "x" = [-2, 2], "mu" = [-1, 1], "sigma" = [1, 3], "epsilon" = [0.01, 0.1] }
nonzero = []
policy = "algebraic"
